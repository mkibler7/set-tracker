{
  
  "name": "rep-tracker",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @reptracker/shared build && npm run -w frontend build",
    "dev": "npm run -w @reptracker/shared build && npm run -w frontend dev"
  }
}
